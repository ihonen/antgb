#ifndef TYPEDEFS_HH
#define TYPEDEFS_HH

#include <cassert>
#include <cstdint>
#include <cstring>
#include <iostream>

using std::cerr;
using std::cout;
using std::endl;
using std::pair;
using std::string;

typedef uint16_t memaddr_t;
typedef uint16_t memsize_t;
typedef uint16_t lcdsize_t;

#endif // TYPEDEFS_HH
